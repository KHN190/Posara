// Reusable chiptune synth kit on the polyphonic synth voices.
// Patches: 0 kick · 1 acid bass · 2 clap · 3 hat · 4 lead pluck · 5 alarm/pad.
// Patches 0..3 are lifted from acid.abe's setup; 4/5 added for melody + stingers.
// Call synthkit_setup() once in start(), then snd_on(patch, midi, vol, dur_ms).

pub fn synthkit_setup() -> <IO> Unit {
  snd_voices(16);

  // 0 = kick: sine with a downward pitch env (the "thump").
  snd_osc(0, 0, 1, 0, 0, 100);
  snd_filter(0, 0, 1400, 8);
  snd_env(0, 0, 0, 100, 0, 110, 0, 25);
  snd_env(0, 1, 2, 30, 0, 55, 0, 0);

  // 1 = 303-ish saw bass through a resonant low-pass filter envelope.
  snd_osc(1, 0, 3, 0, 0, 100);
  snd_filter(1, 0, 520, 88);
  snd_env(1, 0, 0, 100, 2, 260, 35, 80);
  snd_env(1, 1, 1, 65, 2, 150, 0, 70);

  // 2 = clap: noise burst.
  snd_osc(2, 0, 4, 0, 0, 100);
  snd_filter(2, 2, 1600, 40);
  snd_env(2, 0, 0, 100, 0, 120, 0, 60);

  // 3 = hat: bright noise, fast decay.
  snd_osc(3, 0, 4, 0, 0, 90);
  snd_filter(3, 1, 7000, 10);
  snd_env(3, 0, 0, 100, 0, 38, 0, 18);

  // 4 = lead: square pluck for melody / victory fanfare.
  snd_osc(4, 0, 0, 1, 0, 100);
  snd_filter(4, 0, 4200, 14);
  snd_env(4, 0, 0, 100, 2, 95, 0, 70);

  // 5 = alarm/pad: detuned saw, band-pass, for the boss-incoming sting.
  snd_osc(5, 0, 3, 1, 0, 80);
  snd_osc(5, 1, 3, 2, 7, 50);
  snd_filter(5, 2, 800, 60);
  snd_env(5, 0, 0, 100, 20, 200, 60, 220)
}
